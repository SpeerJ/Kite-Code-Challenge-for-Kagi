import 'package:flutter/material.dart';

class FailureCategoryFeed extends StatelessWidget {
  const FailureCategoryFeed({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Failed to load feed');
  }

}